create type gender as enum ('none', 'male', 'female')
;
/*--;--*/

create domain email as varchar(100)
;
/*--;--*/

create domain hash_md5 as varchar(32)
;
/*--;--*/

create domain varchar100 as varchar(100)
;
/*--;--*/

create domain varchar32 as varchar(32)
;
/*--;--*/

create type member_key as enum ('reminder', 'activation', 'mailchange')
;
/*--;--*/

create domain varchar150 as varchar(150)
;
/*--;--*/

create domain varchar200 as varchar(200)
;
/*--;--*/

create domain varchar256 as varchar(256)
;
/*--;--*/

create domain varchar64 as varchar(64)
;
/*--;--*/

create domain varchar128 as varchar(128)
;
/*--;--*/

create domain varchar16 as varchar(16)
;
/*--;--*/

create domain varchar8 as varchar(8)
;
/*--;--*/


